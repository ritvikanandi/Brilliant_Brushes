<%- include('../includes/header.ejs') %>
<link rel="stylesheet" href="/css/cart.css">
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
    <% if (errorMessage.length > 0) { %>
        <div class="user-message user-message--error">
            <%= errorMessage %>
        </div>
        <% } %>
        <% if (success.length > 0) { %>
            <div class="user-message user-message--success">
                <%= success %>
            </div>
            <% } %>
  <main>
    <ul class="cart__item-list">
        <% products.forEach(p => { %>
            <li class="cart__item">
                <h1><%= p.productId.title %></h1>
                <h2>Quantity: <%= p.quantity %></h2>
                <!-- <form action="/cart-delete-item" method="POST">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <input type="hidden" value="<%= p.productId._id %>" name="productId">
                    <button class="btn_delete" type="submit">Delete</button>
                </form> -->
            </li>
        <% }) %>
    </ul>
    <div class="centered">
        <h2 class="lead">Total(₹): <%= totalSum %></h2>
    </div>
    <div class="centered">
        <button id="order-btn" class="btn">Proceed to payment</button>
            <script src="https://js.stripe.com/v3/">
            </script>
            <script>
                var stripe = Stripe('pk_test_51JVA4YSEKduK30PRYQM0CWZOpK5YV7rP7F7dxPS1IpskfSJwqFe4c5PBLI6Hqd1nZWjlbRev30Bd0BNoif2qA7pv00GnqZ27QE');
                var orderBtn = document.getElementById('order-btn');
                orderBtn.addEventListener('click', function(){
                    stripe.redirectToCheckout({
                        sessionId: '<%= sessionId %>',
                    });
                });

            </script>
    </div>
  </main>

<%- include('../includes/footer.ejs') %>